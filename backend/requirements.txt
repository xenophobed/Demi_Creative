# Core Framework
fastapi>=0.110.0,<1
uvicorn[standard]>=0.27.0
pydantic>=2.7.0,<3
pydantic-settings>=2.5.2
python-dotenv>=1.0.1
python-multipart>=0.0.9  # For file upload support

# AI & Agent
claude-agent-sdk==0.1.63
mcp>=1.26.0,<2  # Pin to compatible version (Server accepts version= kwarg)
anthropic>=0.18.1,<1
openai>=1.12.0,<2
replicate>=1.0.7
elevenlabs>=1.0.0
mutagen>=1.47.0  # portable audio duration probing (voice-clone validation)

# Vector Database (Local)
chromadb>=1.4.1

# Vector Database (Production — pgvector)
# Uses OpenAI embeddings (text-embedding-3-small) — no extra deps needed
pgvector>=0.2.0

# Image Processing
pillow>=10.2.0

# HTTP Client
# Pinned <0.28 because httpx 0.28 removed the `proxies=` kwarg that
# anthropic 0.18.1's Client.__init__ still passes internally. Symptom:
# every safety_check_server call raises:
#   "Client.__init__() got an unexpected keyword argument 'proxies'"
# which surfaces as HTTP 503 SAFETY_UNAVAILABLE on the buddy save flow.
httpx>=0.27.0,<0.28

# Web Search (News fetching for Daily Drop)
tavily-python>=0.5.0

# Database
aiosqlite>=0.19.0
asyncpg>=0.29.0

# Auth (Supabase JWT validation)
PyJWT[crypto]>=2.8.0

# Supabase Storage (#343)
supabase>=2.0.0

# Utilities
pyyaml>=6.0.1

# Testing
pytest>=7.4.4
pytest-asyncio>=0.23.4
pytest-cov>=4.1.0
pytest-mock>=3.12.0

# Code Quality
black>=24.1.1
mypy>=1.8.0

# Logging
structlog>=24.1.0
